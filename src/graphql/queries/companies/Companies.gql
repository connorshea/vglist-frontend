query Companies($before: String, $last: Int, $after: String) {
  companies(before: $before, last: $last, after: $after) {
    nodes {
      id
      name
    }
    pageInfo {
      ...paginationFields
    }
  }
}

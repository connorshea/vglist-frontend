query Games($before: String, $after: String, $sortBy: GameSort, $platformId: ID, $byYear: Int) {
  games(before: $before, after: $after, sortBy: $sortBy, onPlatform: $platformId, byYear: $byYear) {
    nodes {
      ...gameCardFields
    }
    pageInfo {
      ...paginationFields
    }
  }
}

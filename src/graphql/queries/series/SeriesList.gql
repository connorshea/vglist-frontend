query SeriesList($before: String, $last: Int, $after: String) {
  seriesList(before: $before, last: $last, after: $after) {
    nodes {
      id
      name
    }
    pageInfo {
      ...paginationFields
    }
  }
}

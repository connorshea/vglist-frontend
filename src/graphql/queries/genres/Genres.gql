query Genres($before: String, $last: Int, $after: String) {
  genres(before: $before, last: $last, after: $after) {
    nodes {
      id
      name
    }
    pageInfo {
      ...paginationFields
    }
  }
}
